"{   \"name\": \"pandas.DataFrame.any\",   \"api_name\": \"pandas.DataFrame.any\",   \"description\": \"Return whether any element is True, potentially over an axis. Returns False unless there is at least one element within a series or along a Dataframe axis that is True or equivalent (e.g. non-zero or non-empty).\",   \"parameters\": {     \"required\": [       {         \"name\": \"axis\",         \"description\": \"Indicate which axis or axes should be reduced. For Series this parameter is unused and defaults to 0.\\n\\n0 / 'index' : reduce the index, return a Series whose index is the original column labels.\\n1 / 'columns' : reduce the columns, return a Series whose index is the original index.\\nNone : reduce all axes, return a scalar.\"       }     ],     \"optional\": [       {         \"name\": \"bool_only\",         \"description\": \"Include only boolean columns. Not implemented for Series.\"       },       {         \"name\": \"skipna\",         \"description\": \"Exclude NA/null values. If the entire row/column is NA and skipna is True, then the result will be False, as for an empty row/column. If skipna is False, then NA are treated as True, because these are not equal to zero.\"       },       {         \"name\": \"**kwargs\",         \"description\": \"Additional keywords have no effect but might be accepted for compatibility with NumPy.\"       }     ]   } }"